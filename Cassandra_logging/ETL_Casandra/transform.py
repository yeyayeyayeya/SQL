def transform_dataframe(df, table_name):
    # Có thể xử lý thêm: đổi kiểu dữ liệu, chuẩn hóa string, mapping, ...
    # Ví dụ: df['some_column'] = df['some_column'].str.upper()
    return df
print("Transform Successfully!")